#ifndef UTILS_H
#define UTILS_H

/**
 * @brief Add two points
 *
 * @param extreme
 * @param arg
 * @return struct Point
 */
typedef struct {
    int extreme;
    int arg;
} extm;

extm max(int*,int*);
extm min(int*,int*);



#endif